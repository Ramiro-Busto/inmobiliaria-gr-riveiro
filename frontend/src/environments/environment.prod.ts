// Entorno de producción. IMPORTANTE: reemplazar apiOrigin por la URL real que Render le
// asigne al backend una vez creado el servicio (algo como "https://gr-riveiro-api.onrender.com"),
// y volver a hacer el deploy en Netlify para que tome el cambio.
export const environment = {
  apiOrigin: 'https://TODO-reemplazar-con-la-url-de-render.onrender.com',
};
