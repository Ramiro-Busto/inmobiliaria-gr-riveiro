// Entorno de desarrollo (ng serve). En producción, Angular reemplaza este archivo
// por environment.prod.ts automáticamente (ver angular.json → fileReplacements).
export const environment = {
  apiOrigin: 'http://localhost:5080',
};
